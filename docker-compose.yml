version: '3'

services:

  app:
    build: ./
    ports:
      - "3000:3000"
      - "3001:3001"

  mongodbcont:
    image: mongo:latest
    ports:
      - "27017:27017"
    volumes:
      - mongodb-data:/data/db

volumes:
  mongodb-data: